// Prisma is reserved here as the persistence integration point for a later phase.
// TODO: replace this placeholder with a generated Prisma client and adapter wiring.
export interface PrismaClientInstance {
  readonly __brand: "PrismaClientInstance";
}

export const prisma = null as unknown as PrismaClientInstance;
